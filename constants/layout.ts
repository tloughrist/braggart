/**
 * Responsive layout tokens. Braggart is mobile-first: design for a phone
 * width, then let content grow to a comfortable max on tablet/web rather than
 * stretching edge-to-edge.
 */
export const Breakpoints = {
  /** below this width we treat the screen as a phone ("compact") */
  compact: 640,
};

/** Max width for a screen's main content column (centered on wider screens). */
export const MAX_CONTENT_WIDTH = 640;
